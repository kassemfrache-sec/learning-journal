# Pre Security Path - Room: DNS in Detail

**Date:** 2026-06-13

**Source:** TryHackMe Pre Security Path

## What I learned

- I learned What is the main purpose of DNS (Domain Name System), how it functions, what's the domain hierarchy(Root, TLD, SLD, Subdomains), Different types of DNS records(A, AAAA, CNAME, MX,TXT) how a DNS request
starts and circles back to your device with the domain(local cache, Recursive server, Root server, TLD server, Authoritative server, Cached for later searches).

## What confused me initially

- What confused me was the types of records especially CNAME records and there connection to A records and how MX (mail exchange) records functioned got a grasp of the concepts after long good research and demonstrative YT videos.

## How it connects

- it actually connects to a special in detail room the HTTP in detail room, how DNS query's a IP address for a specific domain name, how the chain moves from ones web browser (Chrome, Firefox, etc) through the DNS system and until it connects to a web server what stands in the middle of all of this process all connects to the HTTP in detail room and other rooms in the same module.

